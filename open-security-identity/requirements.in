# FastAPI and web framework
fastapi>=0.116.0,<1.0.0
uvicorn[standard]>=0.32.0,<0.33.0

# Database and ORM
sqlalchemy[asyncio]>=2.0.30,<2.1.0
asyncpg>=0.30.0,<0.31.0
psycopg2-binary>=2.9.0,<3.0.0
alembic>=1.14.0,<1.15.0

# Authentication and user management
fastapi-users[sqlalchemy]>=15.0.0,<16.0.0
python-multipart>=0.0.22,<0.1.0
passlib[bcrypt]>=1.7.4,<1.8.0


# Configuration and validation
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
email-validator>=2.1.0,<3.0.0

# HTTP client
httpx>=0.25.0,<0.26.0

# Security - constrain cryptography for compatibility
cryptography>=48.0.1,<49.0.0

# Explicit security constraints for transitive deps
# Capping Starlette below 1.0 also capped FastAPI, leaving identity on its
# own Starlette major (WILDBO-DEP-01). Aligned with the rest of the platform.
starlette>=1.6.0,<2.0.0

# app/token_blacklist.py does `import redis.asyncio`. It was missing here: at
# the audited commit the only importer did it inside a function, so the
# ModuleNotFoundError surfaced as a 500 on the first authenticated request
# instead of at start-up. logout.py imports it at module level, which turned
# the same missing dependency into "the identity service does not start" --
# louder, and the honest failure. The dependency was always required.
redis>=5.0.0,<6.0.0

# The service calls install_observability() and monitoring/prometheus.yml lists
# it as a scrape target, but without this package the shared helper falls back
# to no-op metrics and /metrics answers "prometheus_client is not installed" --
# a target Prometheus polls forever and never gets a series from.
prometheus-client>=0.19.0,<1.0.0
urllib3>=2.6.3,<3.0.0

# Testing
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<0.22.0
black==24.3.0
isort==5.12.0
flake8==6.1.0
