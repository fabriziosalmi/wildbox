# Test Dependencies for Wildbox Security Platform
# Used by CI/CD pipeline and local testing

# Core testing framework
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-html==4.1.1
pytest-timeout==2.3.1
pytest-xdist==3.6.1

# HTTP client for integration tests
requests==2.33.0
httpx==0.27.2

# Direct DB access for cross-tenant seeding tests (data service has no create API)
psycopg2-binary==2.9.10

# tests/chaos/test_chaos_experiments.py imports both at module level, and
# neither was here: the chaos suite collected 0 items and errored on import,
# so it has never actually run. `docker` drives the containers the experiments
# kill and restart; `psutil` reads the host resource pressure they assert on.
docker==7.2.0
# 6.0.0 and not the current 7.x: safety==3.2.11 below pins psutil~=6.0.0, and
# 7.x makes the whole file unresolvable ("ResolutionImpossible"). Bumping this
# means bumping safety too.
psutil==6.0.0

# Async testing support
aiohttp==3.14.1

# Environment management
python-dotenv==1.2.2

# Test data generation
faker==33.1.0

# Code quality
black==24.10.0
flake8==7.1.1
mypy==1.13.0
pylint==3.3.2

# Security scanning
bandit==1.8.0
safety==3.2.11

# Coverage reporting
coverage==7.6.9
