# Wildbox Security Gateway - Environment Configuration
# =====================================================
# SECURITY WARNING: Never commit actual secrets to version control!
# Copy this file to .env and update with your actual values

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================

# Environment mode (development, staging, production)
WILDBOX_ENV=development

# Gateway log level (debug, info, warn, error)
GATEWAY_LOG_LEVEL=info

# ============================================================================
# SECURITY CONFIGURATION (CRITICAL - MUST BE CHANGED IN PRODUCTION!)
# ============================================================================

# Gateway Internal Secret - Used for secure communication with identity service
# CRITICAL: This secret authenticates internal service-to-service requests
# Generate a secure random secret using: openssl rand -hex 32
# NEVER use the default value in production!
GATEWAY_INTERNAL_SECRET=CHANGE-THIS-TO-A-SECURE-RANDOM-SECRET-IN-PRODUCTION

# ============================================================================
# SERVICE URLS
# ============================================================================

# Identity Service URL (authentication and authorization)
# Default when running in Docker: http://open-security-identity:8001
# For standalone testing: http://localhost:8001
IDENTITY_SERVICE_URL=http://open-security-identity:8001

# ============================================================================
# CACHING CONFIGURATION
# ============================================================================

# Authentication Cache TTL (Time To Live) in seconds
# Caches JWT/API key validation results to reduce load on identity service
# Recommended: 300 (5 minutes)
# Range: 60-600 seconds
AUTH_CACHE_TTL=300

# ============================================================================
# DEBUG CONFIGURATION
# ============================================================================

# Enable debug mode (adds debug headers and verbose logging)
# WARNING: Never enable in production! May expose sensitive information
# Values: true, false
GATEWAY_DEBUG=false

# ============================================================================
# PRODUCTION CHECKLIST
# ============================================================================
# Before deploying to production, ensure:
# 1. [ ] GATEWAY_INTERNAL_SECRET is changed to a secure random value
# 2. [ ] WILDBOX_ENV is set to "production"
# 3. [ ] GATEWAY_LOG_LEVEL is set to "info" or "warn"
# 4. [ ] GATEWAY_DEBUG is set to "false"
# 5. [ ] AUTH_CACHE_TTL is appropriate for your security requirements
# 6. [ ] IDENTITY_SERVICE_URL points to your production identity service
# 7. [ ] SSL certificates are from a trusted CA (not self-signed)
# 8. [ ] All default values have been reviewed and customized
